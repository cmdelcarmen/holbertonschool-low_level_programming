#include "lists.h"

/**
 * delete_dnodeint_at_index - deletes node
 * @head: head pointer
 * @index: idx of node to del
 * Return: 1/-1
 */
int delete_dnodeint_at_index(dlistint_t **head, unsigned int index)
{

	if (*head == NULL || index == 0)
	{
		return (-1);
	}

	return (1);
}
